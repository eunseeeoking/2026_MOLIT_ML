# MySQL 연결 문자열 — 2026_MOLIT_CONTEST 와 동일한 DB
DATABASE_URL="mysql://root:root@localhost:3306/molit_contest"

# 학습 옵션 (선택)
# MIN_SAMPLES_PER_BUCKET=20   # 구간당 최소 거래 건수
# WINDOW_SIZE=24              # LSTM 입력 윈도우 (개월)
# HORIZON_MONTHS=36           # 예측 시점 (개월)
# MODEL_VERSION=lstm-v1
